@page
@model Asp.netCore_Palma_RealState.Pages.Admin.Estate.adminModel
@{
}
<div class="row mt-5">
    <div class="col-lg-3 col-md-3 col-12">
        <div class="text-right">
            <div class="list-group p-0 mb-5">
                <a href="#" class="active list-group-item list-group-item-action">
                    مدیریت املاک
                </a>
            </div>
        </div>
    </div>
    <div class="col-lg-9 col-md-9 col-12">
        <div class="card">
            <div class="card-header d-flex flex-wrap">
                <a href="create.html" class="btn btn-primary text-white mr-0">
                    <i class="fas fa-plus"></i>
                    ایجاد ملک جدید
                </a>
                <h4 class="text-center font-weight-bold m-auto"> مدیریت املاک </h4>
            </div>
            <div class="card-body">
                @if (Model.show_all_estate.Any())
                {
                    <table class="table table-bordered table-striped table-hover mt-2">
                        <thead>
                            <tr>
                                <th class="text-center"> عکس </th>
                                <th class="text-center"> نام </th>
                                <th class="text-center"> متراژ </th>
                               @*  <th class="text-center"> دسته بندی</th> *@
                                <th class="text-center">مدیریت</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.show_all_estate)
                            {
                                <tr>
                                    <td class="text-center align-middle"><img src="~/image/img_estate/@item.image" class="img-fluid" width="60" height="60" alt=""></td>
                                    <td class="text-center align-middle">@item.tittle</td>
                                    <td class="text-center align-middle">@item.metrage</td>
                                    @*  <td class="text-center align-middle">آپارتمان</td> *@
                                    <td class="text-center">
                                        <div class="btn-group" dir="ltr" role="group" aria-label="Basic example">
                                            <a asp-page="detail" asp-route-id="@item.ID_estate" class="btn btn-sm btn-info"><i class="fas fa-list"></i></a>
                                            <a asp-page="edit" asp-route-id="@item.ID_estate" class="btn btn-sm btn-warning"> <i class="fas fa-edit"></i> </a>
                                            <a asp-page="delete" asp-route-id="@item.ID_estate" class="btn btn-sm btn-danger"><i class="fas fa-trash"></i></a>
                                        </div>
                                    </td>
                                </tr>
                            }

                        </tbody>
                    </table>

                }
                else
                {
                    <div class="alert alert-warning" role="alert">
                        ملکی موجود نیست <a href="#" class="alert-link">کلیک کنید</a>. 
                    </div>
                }


            </div>

        </div>
    </div>
</div>
